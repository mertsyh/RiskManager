# Supabase — copy to `.env` and fill in (Project Settings → API).
# The anon key is publishable (client-safe) because Row Level Security restricts what it can do.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
